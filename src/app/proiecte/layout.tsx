import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Proiecte",
};

export default function ProiecteLayout({ children }: { children: React.ReactNode }) {
    return children;
}
